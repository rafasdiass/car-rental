import { FormatPlatePipe } from './format-plate.pipe';

describe('FormatPlatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlatePipe();
    expect(pipe).toBeTruthy();
  });
});
